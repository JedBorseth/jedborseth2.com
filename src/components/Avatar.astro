---
import { cn } from "../lib/utils";

const img = Astro.props.img;
const className = Astro.props.className;
---

<div
  style={{
    backgroundImage: `url(${img})`,
  }}
  class={cn(
    "h-56 w-56 rounded-full border-2 border-black bg-cover bg-center",
    className
  )}
  ,
>
</div>
